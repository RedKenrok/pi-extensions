{
	"name": "pi-extensions",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"engines": {
		"node": ">=22.19.0"
	},
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"postinstall": "node scripts/bundle-shared.mjs --link-all",
		"link:shared": "node scripts/bundle-shared.mjs --link-all",
		"ci": "npm run lint && npm run test && npm run test:coverage && node scripts/verify-packages.mjs",
		"lint": "biome check --error-on-warnings package.json tsconfig.json packages scripts test-support && tsc --noEmit",
		"lint:fix": "biome check --write package.json tsconfig.json packages scripts test-support && tsc --noEmit",
		"test": "npm run test --workspaces",
		"test:coverage": "npm run test:coverage --workspaces"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.14",
		"@earendil-works/pi-ai": "0.85.1",
		"@earendil-works/pi-coding-agent": "0.85.1",
		"@earendil-works/pi-tui": "0.85.1",
		"@types/node": "24.5.2",
		"@types/turndown": "5.0.6",
		"typescript": "5.9.2"
	}
}
