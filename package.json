{
	"name": "pi-extensions",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"engines": {
		"node": ">=22.19.0"
	},
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"ci": "biome check package.json tsconfig.json packages/codex-research-tool packages/fetch-tool packages/codex-compaction && tsc --noEmit && npm run test && npm run test:coverage",
		"lint": "biome check --write package.json tsconfig.json packages/codex-research-tool packages/fetch-tool packages/codex-compaction && tsc --noEmit",
		"test": "npm run test --workspaces --if-present",
		"test:coverage": "npm run test:coverage --workspaces --if-present"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.14",
		"@earendil-works/pi-coding-agent": "0.85.1",
		"@earendil-works/pi-tui": "0.85.1",
		"@types/node": "24.5.2",
		"@types/turndown": "5.0.6",
		"typescript": "5.9.2"
	}
}
